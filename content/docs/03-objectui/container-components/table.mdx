---
title: Table
description: Data table with sorting, filtering, and pagination
---

# Table

Data table with sorting, filtering, and pagination.

## Protocol

```typescript
{
  type: 'table',
  api?: string,               // Data endpoint
  data?: any[],               // Static data
  columns: Array<{
    name: string,
    label: string,
    type?: 'text' | 'number' | 'boolean' | 'date' | 'image',
    sortable?: boolean,
    filterable?: boolean,
    width?: number | string,
    render?: string,          // Custom render template
  }>,
  rowActions?: Action[],      // Actions per row
  batchActions?: Action[],    // Batch actions
  pagination?: boolean,
  pageSize?: number,
  searchable?: boolean,       // Global search
  filters?: Array<{
    name: string,
    label: string,
    type: 'input' | 'select' | 'datepicker'
  }>,
  selectable?: boolean,       // Row selection
  expandable?: Component,     // Expandable row content
}
```

## Examples

### Basic Table

```json
{
  "type": "table",
  "api": "/api/users",
  "columns": [
    { "name": "id", "label": "ID", "width": 80 },
    { "name": "name", "label": "Name", "sortable": true },
    { "name": "email", "label": "Email", "sortable": true },
    { "name": "role", "label": "Role", "filterable": true },
    { "name": "createdAt", "label": "Created", "type": "date", "sortable": true }
  ],
  "rowActions": [
    { "label": "Edit", "onClick": { "type": "navigate", "url": "/users/${id}/edit" } },
    { "label": "Delete", "onClick": { "type": "api", "api": "/api/users/${id}", "method": "DELETE", "confirm": "Delete this user?" } }
  ],
  "pagination": true,
  "pageSize": 20,
  "searchable": true
}
```

### CRUD Table

```json
{
  "type": "table",
  "api": "/api/products",
  "columns": [
    { "name": "name", "label": "Product Name" },
    { "name": "price", "label": "Price", "type": "number" },
    { "name": "stock", "label": "Stock", "type": "number" },
    { "name": "active", "label": "Active", "type": "boolean" }
  ],
  "filters": [
    { "name": "category", "label": "Category", "type": "select", "api": "/api/categories" },
    { "name": "priceRange", "label": "Price Range", "type": "input" }
  ],
  "selectable": true,
  "batchActions": [
    { "label": "Delete Selected", "onClick": { "type": "api", "api": "/api/products/batch-delete", "method": "POST" } },
    { "label": "Export", "onClick": { "type": "download", "url": "/api/products/export" } }
  ],
  "rowActions": [
    { "label": "Edit", "onClick": { "type": "dialog", "title": "Edit Product", "body": { "type": "form", "fields": [...] } } },
    { "label": "Delete", "onClick": { "type": "api", "api": "/api/products/${id}", "method": "DELETE" } }
  ]
}
```

### Static Data Table

```json
{
  "type": "table",
  "data": [
    { "id": 1, "name": "Alice", "role": "Admin" },
    { "id": 2, "name": "Bob", "role": "User" }
  ],
  "columns": [
    { "name": "id", "label": "ID" },
    { "name": "name", "label": "Name" },
    { "name": "role", "label": "Role" }
  ]
}
```

## Enterprise Use Cases

### Employee Management Table

```json
{
  "type": "table",
  "api": "/api/employees",
  "columns": [
    { "name": "employeeId", "label": "Employee ID", "width": 120, "sortable": true },
    { "name": "fullName", "label": "Full Name", "sortable": true },
    { "name": "department", "label": "Department", "filterable": true },
    { "name": "position", "label": "Position", "filterable": true },
    { "name": "hireDate", "label": "Hire Date", "type": "date", "sortable": true },
    { "name": "salary", "label": "Salary", "type": "number" },
    { "name": "isActive", "label": "Status", "type": "boolean" }
  ],
  "filters": [
    { "name": "department", "label": "Department", "type": "select", "api": "/api/departments" },
    { "name": "position", "label": "Position", "type": "select", "api": "/api/positions" },
    { "name": "status", "label": "Status", "type": "select", "options": [
      { "label": "Active", "value": "active" },
      { "label": "Inactive", "value": "inactive" }
    ]}
  ],
  "rowActions": [
    { "label": "View", "onClick": { "type": "navigate", "url": "/employees/${id}" } },
    { "label": "Edit", "onClick": { "type": "dialog", "title": "Edit Employee", "body": { "type": "form", "fields": [...] } } },
    { "label": "Deactivate", "onClick": { "type": "api", "api": "/api/employees/${id}/deactivate", "method": "POST", "confirm": "Deactivate this employee?" } }
  ],
  "pagination": true,
  "pageSize": 25,
  "searchable": true
}
```

### Invoice Management Table

```json
{
  "type": "table",
  "api": "/api/invoices",
  "columns": [
    { "name": "invoiceNumber", "label": "Invoice #", "width": 150, "sortable": true },
    { "name": "customerName", "label": "Customer", "sortable": true },
    { "name": "invoiceDate", "label": "Invoice Date", "type": "date", "sortable": true },
    { "name": "dueDate", "label": "Due Date", "type": "date", "sortable": true },
    { "name": "amount", "label": "Amount", "type": "number", "sortable": true },
    { "name": "status", "label": "Status", "filterable": true }
  ],
  "filters": [
    { "name": "status", "label": "Status", "type": "select", "options": [
      { "label": "Draft", "value": "draft" },
      { "label": "Sent", "value": "sent" },
      { "label": "Paid", "value": "paid" },
      { "label": "Overdue", "value": "overdue" }
    ]},
    { "name": "dateRange", "label": "Date Range", "type": "datepicker", "mode": "daterange" }
  ],
  "selectable": true,
  "batchActions": [
    { "label": "Send Selected", "onClick": { "type": "api", "api": "/api/invoices/batch-send", "method": "POST" } },
    { "label": "Download PDF", "onClick": { "type": "download", "url": "/api/invoices/batch-download" } }
  ],
  "rowActions": [
    { "label": "View", "onClick": { "type": "navigate", "url": "/invoices/${id}" } },
    { "label": "Send", "onClick": { "type": "api", "api": "/api/invoices/${id}/send", "method": "POST" } },
    { "label": "Download", "onClick": { "type": "download", "url": "/api/invoices/${id}/pdf" } }
  ],
  "pagination": true,
  "pageSize": 20,
  "searchable": true
}
```

### Inventory Management Table

```json
{
  "type": "table",
  "api": "/api/inventory",
  "columns": [
    { "name": "sku", "label": "SKU", "width": 120, "sortable": true },
    { "name": "productName", "label": "Product Name", "sortable": true },
    { "name": "category", "label": "Category", "filterable": true },
    { "name": "quantity", "label": "Quantity", "type": "number", "sortable": true },
    { "name": "unitPrice", "label": "Unit Price", "type": "number", "sortable": true },
    { "name": "totalValue", "label": "Total Value", "type": "number", "sortable": true },
    { "name": "lastRestocked", "label": "Last Restocked", "type": "date" }
  ],
  "filters": [
    { "name": "category", "label": "Category", "type": "select", "api": "/api/categories" },
    { "name": "lowStock", "label": "Low Stock Only", "type": "switch" }
  ],
  "selectable": true,
  "batchActions": [
    { "label": "Adjust Stock", "onClick": { "type": "dialog", "body": { "type": "form", "fields": [...] } } },
    { "label": "Export", "onClick": { "type": "download", "url": "/api/inventory/export" } }
  ],
  "rowActions": [
    { "label": "Edit", "onClick": { "type": "dialog", "title": "Edit Product", "body": { "type": "form", "fields": [...] } } },
    { "label": "Restock", "onClick": { "type": "dialog", "title": "Restock", "body": { "type": "form", "fields": [...] } } }
  ],
  "pagination": true,
  "pageSize": 30,
  "searchable": true
}
```

### Purchase Order Table

```json
{
  "type": "table",
  "api": "/api/purchase-orders",
  "columns": [
    { "name": "poNumber", "label": "PO Number", "width": 150, "sortable": true },
    { "name": "supplierName", "label": "Supplier", "sortable": true },
    { "name": "orderDate", "label": "Order Date", "type": "date", "sortable": true },
    { "name": "deliveryDate", "label": "Delivery Date", "type": "date", "sortable": true },
    { "name": "totalAmount", "label": "Total Amount", "type": "number", "sortable": true },
    { "name": "status", "label": "Status", "filterable": true },
    { "name": "approvalStatus", "label": "Approval", "filterable": true }
  ],
  "filters": [
    { "name": "status", "label": "Status", "type": "select", "options": [
      { "label": "Draft", "value": "draft" },
      { "label": "Submitted", "value": "submitted" },
      { "label": "Approved", "value": "approved" },
      { "label": "Received", "value": "received" }
    ]},
    { "name": "supplier", "label": "Supplier", "type": "select", "api": "/api/suppliers" }
  ],
  "rowActions": [
    { "label": "View", "onClick": { "type": "navigate", "url": "/purchase-orders/${id}" } },
    { "label": "Approve", "onClick": { "type": "api", "api": "/api/purchase-orders/${id}/approve", "method": "POST", "confirm": "Approve this purchase order?" } },
    { "label": "Cancel", "onClick": { "type": "api", "api": "/api/purchase-orders/${id}/cancel", "method": "POST", "confirm": "Cancel this purchase order?" } }
  ],
  "pagination": true,
  "pageSize": 20
}
```

### Expense Report Table

```json
{
  "type": "table",
  "api": "/api/expense-reports",
  "columns": [
    { "name": "reportId", "label": "Report ID", "width": 100, "sortable": true },
    { "name": "employeeName", "label": "Employee", "sortable": true },
    { "name": "reportTitle", "label": "Title", "sortable": true },
    { "name": "submissionDate", "label": "Submitted", "type": "date", "sortable": true },
    { "name": "totalAmount", "label": "Amount", "type": "number", "sortable": true },
    { "name": "status", "label": "Status", "filterable": true }
  ],
  "filters": [
    { "name": "status", "label": "Status", "type": "select", "options": [
      { "label": "Pending", "value": "pending" },
      { "label": "Approved", "value": "approved" },
      { "label": "Rejected", "value": "rejected" },
      { "label": "Paid", "value": "paid" }
    ]},
    { "name": "dateRange", "label": "Submission Date", "type": "datepicker", "mode": "daterange" }
  ],
  "selectable": true,
  "batchActions": [
    { "label": "Approve Selected", "onClick": { "type": "api", "api": "/api/expense-reports/batch-approve", "method": "POST" } },
    { "label": "Export", "onClick": { "type": "download", "url": "/api/expense-reports/export" } }
  ],
  "rowActions": [
    { "label": "Review", "onClick": { "type": "navigate", "url": "/expense-reports/${id}" } },
    { "label": "Approve", "onClick": { "type": "api", "api": "/api/expense-reports/${id}/approve", "method": "POST" } },
    { "label": "Reject", "onClick": { "type": "dialog", "title": "Reject Report", "body": { "type": "form", "fields": [{ "type": "input", "name": "reason", "label": "Rejection Reason" }] } } }
  ],
  "pagination": true,
  "pageSize": 25,
  "searchable": true
}
```

### Leave Request Table

```json
{
  "type": "table",
  "api": "/api/leave-requests",
  "columns": [
    { "name": "requestId", "label": "Request ID", "width": 100 },
    { "name": "employeeName", "label": "Employee", "sortable": true },
    { "name": "leaveType", "label": "Leave Type", "filterable": true },
    { "name": "startDate", "label": "Start Date", "type": "date", "sortable": true },
    { "name": "endDate", "label": "End Date", "type": "date" },
    { "name": "days", "label": "Days", "type": "number" },
    { "name": "status", "label": "Status", "filterable": true }
  ],
  "filters": [
    { "name": "status", "label": "Status", "type": "select", "options": [
      { "label": "Pending", "value": "pending" },
      { "label": "Approved", "value": "approved" },
      { "label": "Rejected", "value": "rejected" }
    ]},
    { "name": "leaveType", "label": "Leave Type", "type": "select", "options": [
      { "label": "Annual Leave", "value": "annual" },
      { "label": "Sick Leave", "value": "sick" },
      { "label": "Personal Leave", "value": "personal" }
    ]}
  ],
  "rowActions": [
    { "label": "View", "onClick": { "type": "navigate", "url": "/leave-requests/${id}" } },
    { "label": "Approve", "onClick": { "type": "api", "api": "/api/leave-requests/${id}/approve", "method": "POST" } },
    { "label": "Reject", "onClick": { "type": "api", "api": "/api/leave-requests/${id}/reject", "method": "POST" } }
  ],
  "pagination": true,
  "pageSize": 20
}
```

### Asset Management Table

```json
{
  "type": "table",
  "api": "/api/assets",
  "columns": [
    { "name": "assetTag", "label": "Asset Tag", "width": 120, "sortable": true },
    { "name": "assetName", "label": "Asset Name", "sortable": true },
    { "name": "category", "label": "Category", "filterable": true },
    { "name": "assignedTo", "label": "Assigned To", "sortable": true },
    { "name": "purchaseDate", "label": "Purchase Date", "type": "date", "sortable": true },
    { "name": "purchasePrice", "label": "Purchase Price", "type": "number" },
    { "name": "warrantyExpiry", "label": "Warranty Expiry", "type": "date" }
  ],
  "filters": [
    { "name": "category", "label": "Category", "type": "select", "options": [
      { "label": "Computer", "value": "computer" },
      { "label": "Furniture", "value": "furniture" },
      { "label": "Vehicle", "value": "vehicle" }
    ]},
    { "name": "assigned", "label": "Assignment Status", "type": "select", "options": [
      { "label": "Assigned", "value": "assigned" },
      { "label": "Available", "value": "available" }
    ]}
  ],
  "rowActions": [
    { "label": "View", "onClick": { "type": "navigate", "url": "/assets/${id}" } },
    { "label": "Assign", "onClick": { "type": "dialog", "title": "Assign Asset", "body": { "type": "form", "fields": [...] } } },
    { "label": "Maintain", "onClick": { "type": "dialog", "title": "Log Maintenance", "body": { "type": "form", "fields": [...] } } }
  ],
  "pagination": true,
  "pageSize": 30,
  "searchable": true
}
```

## Features

### Columns

Each column can specify:

- **type** - Data type for rendering (text, number, boolean, date, image)
- **sortable** - Enable sorting for this column
- **filterable** - Enable filtering for this column
- **width** - Fixed column width

### Sorting

Enable sorting on columns with `sortable: true`. Users can click column headers to sort.

### Filtering

Add filters for specific columns or use global search with `searchable: true`.

### Pagination

Enable pagination with `pagination: true` and set items per page with `pageSize`.

### Row Actions

Actions that apply to individual rows (edit, delete, view details, etc.).

### Batch Actions

Actions that apply to selected rows when `selectable: true`.

### Expandable Rows

Show additional details by setting `expandable` to a component definition.

## Column Types

- **text** - Plain text (default)
- **number** - Numeric values with proper alignment
- **boolean** - Checkmark or yes/no indicator
- **date** - Formatted date display
- **image** - Image thumbnail

## Related Components

- **[Page](/docs/03-objectui/container-components/page)** - Page container
- **[Form](/docs/03-objectui/container-components/form)** - Edit forms
